/**
 * Primary navigation destinations shared by the desktop {@link AppRail} and the
 * mobile {@link AppBottomTabs}. These map directly onto `app.setActiveSection`.
 */
export type AppRailDestination = "chats" | "roles" | "worlds";
