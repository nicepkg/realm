# Runtime State

Realm writes local state snapshots, role memory, simulation forks, and replay artifacts here.

This directory is intentionally project-local runtime output. Keep portable seed state under `worlds/<world>/initial-state.yaml`.
